import React from 'react';
import Chart from 'chart.js/auto';

const Graph = ({data}) => {
  console.log(data);
  return (
    <div>
      <h1>Here should be the chart</h1>
    </div>
  );
};

export default Graph;